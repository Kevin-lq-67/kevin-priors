spring.application.name=kevin-priors

# Honor the PORT env var when deployed (e.g. Render); fall back to 8080 locally.
server.port=${PORT:8080}

# Anthropic Claude API key
anthropic.api.key=${ANTHROPIC_API_KEY}